# Nexus - Python Dependencies (Python 3.10+)

# Core framework
fastapi==0.103.2
uvicorn[standard]==0.24.0

# Pydantic v2 (schemas, settings)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Anthropic SDK
anthropic==0.39.0

# HTTP client for Ollama
httpx==0.27.2

# Async HTTP and HTML parsing for skills (research-conductor)
aiohttp>=3.9.0
beautifulsoup4>=4.12.0

# Database (PostgreSQL via SQLAlchemy async)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
aiosqlite==0.19.0  # kept for SQLite->PG migration script

# Telegram bot (optional)
python-telegram-bot==21.3

# Environment management
python-dotenv==1.0.1

# Encryption for secrets
cryptography>=41.0.0

# Markdown processing for skills
pyyaml==6.0.1

# Text analysis for routing
tiktoken>=0.7.0

# JWT authentication
PyJWT>=2.8.0

# System monitoring (health checks)
psutil>=5.9.0

# Mem0 long-term memory
mem0ai>=1.0.0

# Python 3.9 compatibility for PEP 604 type unions in Pydantic
eval_type_backport>=0.2.0
